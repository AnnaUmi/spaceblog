extends ../_template
block content
	.wrapper
		.content
			include ../common/_header
			section.section
				.container
					ul.article-wrap
						each index in items
							li.article
								.article__title= index.title
								.article__data= index.data
								.article__text= index.body
